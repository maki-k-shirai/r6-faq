# DESIGN.md — r6-faq デザインルール

---

## カラールール

### プライマリカラー
- ヘッダー背景・検索ボタン・主要アクション：`bg-pink-900`（`#751a3d`）
  - 実装値はコード内ヘッダー背景色（`Header.tsx` の `bg-pink-900`）を参照すること
- hover状態：`hover:bg-pink-950`（同色を10%暗くする）

### 使用禁止
- プライマリカラー（`pink-900`）以外の塗りつぶしボタンをページ内に混在させない
- 赤系色はプライマリカラーの1色（`pink-900`）に統一する
